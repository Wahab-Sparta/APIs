This weather API gets the current weather of a given postcode or multiple postcodes.
To use this weather api:
1. Create a file called "weather_api_key" and store your API key in there. You can get an API key from: https://openweathermap.org/api
2. Use this format for the body:
    {"postcodes" : ["<postcode>","<postcode>"]}